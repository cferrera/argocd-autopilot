apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: webapp-prod
  name: simple-deployment
spec:
  selector:
    matchLabels:
      app: webapp-prod
  template:
    metadata:
      labels:
        app: webapp-prod
