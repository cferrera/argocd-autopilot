apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: webapp-test
  name: simple-deployment
spec:
  selector:
    matchLabels:
      app: webapp-test
  template:
    metadata:
      labels:
        app: webapp-test
